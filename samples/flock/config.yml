raider-1:
  wasm-file: ./raider/raider.wasm
  wasm-function: handle
  http-port: 8001
  log-level: info
  config: '{"basestar":"http://localhost:9000","name":"raider-1","http-port":"8001"}'
  #allow-hosts: '["localhost", "127.0.0.1"]'
  #allow-paths: '{"path":"/"}'
  #config: '{"name":"hello"}'

raider-2:
  wasm-file: ./raider/raider.wasm
  wasm-function: handle
  http-port: 8002
  log-level: info
  config: '{"basestar":"http://localhost:9000","name":"raider-2","http-port":"8002"}'
  #allow-hosts: '["localhost", "127.0.0.1"]'
  #allow-paths: '{"path":"/"}'
  #config: '{"name":"hello"}'

basestar-mother:
  wasm-file: ./basestar/basestar.wasm
  wasm-function: handle
  http-port: 9000
  log-level: info
  config: '{"name":"basestar"}'


# This is 🚧 work in progress
service-discovery:
  http-port: 8888
